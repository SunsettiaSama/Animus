/**
 * Re-export Vanilla contract (single source of truth).
 * Do not add URLs here — extend ../static/js/api.js first.
 */
export { PATHS, http, wsFactory, pollUntilReady } from '../../../static/js/api.js';
