# =============================================================================
# requirements-light.txt — Docker 默认构建（API 推理模式）
#
# 与 requirements.txt 的区别：
#   - 去除 faiss-cpu    （LTM 已迁移至 Qdrant；离线 CSV 建库工具不含在容器中）
#   - 去除 accelerate   （本地 LLM 加速库，API 模式下无需）
#   - transformers 由 sentence-transformers 自动拉取，不再显式列出
#
# 若需在容器内运行本地 HuggingFace LLM，切换到完整依赖：
#   docker build --build-arg REQUIREMENTS=requirements.txt ...
# =============================================================================

# Web server
fastapi
uvicorn[standard]
pydantic

# LangChain core
langchain-core
langchain-community
langchain-openai

# Embeddings（知识库 + 长期记忆均使用本地 BGE 向量化）
torch
sentence-transformers

# HuggingFace Hub
huggingface-hub

# Utilities
pyyaml
numpy
httpx
html2text

# Knowledge base
pymysql
redis
qdrant-client

# TTS (default provider)
edge-tts
