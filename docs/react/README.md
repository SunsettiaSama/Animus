# react ? ??????

ReAct ?????????????????????????????

---

## ????

```
src/react/
??? loop.py              # ConvLoop ? ????????
??? tao.py               # TaoLoop  ? ?? TAO ???? + ???
??? parser.py            # ReActOutputParser ? LLM ????
?
??? memory/              # ???????L1 ?? / L2 ??? / L3 ???
?   ??? memory.py        # Step + Memory ??????
?   ??? processor.py     # MemoryProcessor ? ?????? milestone?
?   ??? short_term/      # L1 ???Token ????
?   ??? medium_term/     # ?????LLM ????????????
?   ??? long_term/       # L3 ???BGE Embedding + FAISS + RAG
?   ?   ??? memory.py
?   ?   ??? store.py     # LongTermStore???? + ??? + ????
?   ?   ??? init/        # make_memory ????
?   ?   ??? retrieve/    # Retriever + ????????? TIMELINE?
?   ??? milestone/       # L2 ????????????????
?       ??? entry.py     # MilestoneEntry ????
?       ??? store.py     # MilestoneStore?JSON ????
?       ??? retriever.py # MilestoneRetriever?????????
?       ??? scorer.py    # ImportanceScorer?LLM ??????
?       ??? memory.py    # MilestoneMemory ???
?       ??? init.py      # make_milestone ????
?
??? prompt/              # Prompt ???????
?   ??? block.py         # PromptBlock ?? + 5 ????
?   ??? manager.py       # PromptManager ? ????? + ????
?   ??? builder.py       # PromptBuilder ? ?????????
?   ??? template.py      # ReActTemplate ????
?
??? persona/             # ???????"??-??-??"?????
?   ??? engine.py        # EvolutionEngine ? ???????
?   ??? manager.py       # PersonaManager  ? ??????
?   ??? profile/         # ???????? + ??? + ??
?   ?   ??? profile.py   #   PersonaProfile ? ????
?   ?   ??? skills.py    #   Skill + SkillsLibrary ? ?????
?   ?   ??? evolver.py   #   PersonaEvolver ? LLM ???
?   ?   ??? block.py     #   ProfileBlock / SkillsBlock / ReflectionBlock
?   ?   ??? store.py     #   ProfileStore?profile / skills / reflection?
?   ??? preference/      # ???????????/????/?????
?       ??? preference.py #  ShortTermPreference ????
?       ??? updater.py   #   PreferenceUpdater?LLM ????
?       ??? block.py     #   PreferenceBlock?Prompt ????
?
??? trace/               # ?????
    ??? store.py         # TraceStore ? ?? .react/traces/
```

---

## ??????

```
ConvLoop??????
?  ???????????????
?
?  ??????
?       ?
?       ?
?  TaoLoop.stream(question)
?       ?
?       ?  ?? max_steps ?
?       ?       ?
?       ?       ?? recall(include_long_term=(i==0)) ? MemoryResult
?       ?       ?? build_messages / build_messages_from_static ? list[BaseMessage]
?       ?       ?? LLM.stream()     ? raw_output????
?       ?       ?? parse()          ? (thought, action, action_input)
?       ?       ?
?       ?       ?? [finish] ? ? _pending_finish?yield FinishEvent ? ???????
?       ?       ?? [tool]   ? executor.run() ? processor.add(Step)
?       ?
?       ?
?  FinishEvent.answer???????????
?
?  ? ?? post_process() ??????? ?
?       commit + trace + persona + add_turn + consolidate?
?       + build_static() ? _static_cache??????
?
?  ????????????? _static_cache ?????
```

---

## TaoLoop ??????`__init__` ?????

| ?? | ?? | ???? | ?? |
|---|---|---|---|
| `_manager` | `PromptManager` | TaoLoop ? | ?? Prompt ??????? |
| `_long_term` | `LongTermMemory \| None` | TaoLoop ??**??**?| L3 FAISS ????????? |
| `_milestone` | `MilestoneMemory \| None` | TaoLoop ??**??**?| L2 ????????????????? |
| `_trace_store` | `TraceStore \| None` | TaoLoop ? | ????? `.react/traces/` |
| `_persona` | `PersonaManager \| None` | TaoLoop ? | ???? + ??? + ?? |
| `_static_cache` | `StaticPromptParts \| None` | TaoLoop ? | ?? post_process ?????? Prompt ?? |
| `_pending_finish` | `_PendingFinish \| None` | ????? | stream() ???post_process() ?? |
| `processor` | `MemoryProcessor` | **?????** | ?????????? long_term + milestone |

---

### ????????

### ? finish ??

```
? ???????L3 ???????????????
      recall_query = persona.bias_query(question)   ? ?????????

? processor.recall(recall_query, include_long_term=(i==0))
      ?? short_term  ? list[Step]  ????????????
      ?? medium_term ? str         ????????????
      ?? long_term   ? str         ?i==0 ??? L3 FAISS ????????????
      ?? milestone   ? str         ?i==0 ??? L2 ???????????????

? Prompt ????????
      # ?? L3 ? L2 ????????????
      lt_combined = long_term + "\n\n" + milestone

      ? _static_cache ? manager.build_messages_from_static(cache, question, lt_combined, short_term)
                              ? system = cache.system_without_lt + lt_combined ???????
                              ? history = cache.history????
      ? _static_cache ? manager.build_messages(question, result(lt=lt_combined), extra_system_blocks=persona_blocks)
                              ? ???? system/persona/medium_term

      system_blocks??????:
        [SystemBlock]              ? ReAct ?? + ????????
        [ProfileBlock]             ? ?????persona ????
        [SkillsBlock]              ? ????skills_enabled ??
        [ReflectionBlock]          ? ???????reflection_enabled ??????
        [PreferenceBlock]          ? ????????preference_enabled ?? neutral ??
        [MemoryBlock: lt_combined] ? L3 ???? + L2 ???????????
        [MemoryBlock: medium]      ? ????????
      human_blocks:
        [QuestionBlock]          ? ???? + ??????
        [StepsBlock]             ? ??? TAO ????????
        [SuffixBlock]            ? "Thought:"????
      ? list[BaseMessage]

? llm.stream_generate_messages(messages)
      ? ???? chunk?yield ChunkEvent
      ? ?? raw_output

? parse_llm_output(raw_output)
      ? (thought, action, action_input)

? executor.run({"action": action, "args": action_input})
      ? observation?????

? processor.add(Step(thought, action, action_input, observation))
      ?? _trace.append(step)           ? ?????????
      ?? short.add(step)               ? evicted: list[Step]
      ?? medium.absorb(evicted)
              ?? _pending.extend(evicted)
              ?? len(_pending) >= distill_trigger_steps ? _distill()
                       ?? LLM ? ?? _distillate?_pending ??

? yield StepEvent(...)
```

### finish ?????????

```
???? ????stream() ?????????????????

self._pending_finish = _PendingFinish(question, answer, processor, persona_blocks)
yield FinishEvent(answer=answer)   ? ??????????WebSocket ??

???? ????post_process()????????????????

processor.commit(question, answer)
      ?? medium.flush()                ? ?????? _pending
      ?? long.add(Q + Steps + Distillate + A)
      ?  long.save()                   ? ? memories.json + FAISS ??
      ?? milestone.try_add(question, answer, trace)
            ?? LLM ??????0.0?1.0?
            ?? ? >= threshold ? milestone.save() ? milestones.json

trace_store.write(question, answer, processor.trace)
      ?? ? .react/traces/{timestamp}_{slug}.json

persona.evolve(question, answer, processor.trace)
      ?? EvolutionEngine.run(...)
      ?    ?? ? profile    ? LLM ?? ? ProfileDelta ? traits/values/style ???
      ?    ?? ? skills     ? LLM ?? ? SkillDelta ? ???????????
      ?    ?? ? reflect    ? LLM ???????????? reflect_interval ??
      ?? preference_updater.update(preference, question, answer)
               ?? LLM ???? ? ?? mood / topic_interests / style_shifts
                  ?? preference_update_every_n ?????? preference.json?

manager.add_turn(question, answer)
      ?? _history ?? [HumanMessage, AIMessage]

_maybe_consolidate()
      ?? turn_count % consolidation_k == 0 ?
              ?? manager.recent_turns(k) ? long.add(??????) ? long.save()

self._static_cache = manager.build_static(          ? ?????? Prompt
    medium_term=processor.medium_distillate,
    extra_system_blocks=persona_blocks,
)
```

---

## ?? Prompt ???`StaticPromptParts`?

?? `post_process()` ?????**????????**? Prompt ??????????

```python
@dataclass
class StaticPromptParts:
    system_without_lt: str       # base_system + persona + medium_term????????
    history: list[BaseMessage]   # add_turn() ??????
```

????????????
1. ???????????????????
2. ? `long_term` ???? `system_without_lt`
3. ?? `question / short_term / suffix` ? Human ??

?? Prompt ???"????"??"?????"??? Prompt ???

---

## ???????

### ????

| ?? | ?? | ? | ?? | ???? | ???? |
|---|---|---|---|---|---|
| L1 | ?? | `ShortTermMemory` | ?? deque?token ?????| ? ???Human ?? StepsBlock?| ?? |
| ? | ???? | `MediumTermMemory` | ??????LLM ???| ? ????System MemoryBlock?| ??? |
| L2 | ??? | `MilestoneMemory` | JSON ?? + ????? | ? ?????? L3 ?????| ??????? |
| L3 | ?? | `LongTermMemory` | FAISS + JSON ?? | ? ?????System MemoryBlock?| ??????? |

**????**? L1?L3 ?????? L1 ????????? LLM ?????????? `commit()` ?? L3?

### L2 ???????

L2 ??**???????**????????

```
???commit() ?
  ImportanceScorer.score(Q, A, trace)
    ?? LLM ????? 0.0-1.0
    ?? >= threshold ? MilestoneEntry(summary, detail, keywords, emotion, importance)
                        ? MilestoneStore.add() ? milestones.json

???i==0 ?
  MilestoneRetriever.retrieve(entries, query, top_k)
    ?? ?????????? ? ?? top_k ?
         ? ???? "[DATE][emotion] summary" ??? lt_combined
```

### L3 ????????

?????????????? `[YYYY-MM-DD HH:MM UTC]`?LLM ?????"??????????"??????????

| ?? | ???? | ?? |
|---|---|---|
| LIGHT | ?? | ???????? |
| HEAVY | "??/??/???" | ?? top_k????? |
| SUPPLEMENT | ????? | ?????? |
| PROFILE | ???? | ?????? |
| TIMELINE | "??/??/recently?" | ????????? N ???? FAISS?|

### MemoryProcessor ????

```python
# TaoLoop ?????? + ?????????
processor = MemoryProcessor(cfg, llm, long_term=self._long_term, milestone=self._milestone)

# ???????????????
processor = MemoryProcessor(cfg, llm)
```

---

## Prompt ?????

### ?????

```
PromptBlock (ABC)
  render() -> str | None    ? None ??????
?
??? SystemBlock(text)              ? ReAct ?? + ????
??? MemoryBlock(header, sep, content) ? ?/??????????
??? QuestionBlock(prefix, question)   ? ????
??? StepsBlock(format, steps)         ? TAO ????????
??? SuffixBlock(text)                 ? "Thought:"
?
??? persona/??? PromptBlock?
    ??? ProfileBlock(profile)               ? ????
    ??? SkillsBlock(skills, top_k=5)        ? ?????? K ???
    ??? ReflectionBlock(reflection)         ? ???????IROTE?
    ??? PreferenceBlock(preference)         ? ????????? neutral ????
```

### ?? build ??

```
? _static_cache?? 2 ???:
  build_messages_from_static(static, question, long_term, short_term)
    ?? system = static.system_without_lt + [long_term ??]
    ?? history = static.history???????? add_turn?
    ?? human  = question + short_term steps + suffix

? _static_cache?????????:
  build_messages(question, result, extra_system_blocks=persona_blocks)
    ?? ??????????????
```

### ????

```
manager.add_turn(q, a)    ?  _history.append([HumanMessage, AIMessage])
manager.recent_turns(k)   ?  list[(question, answer)]  ? consolidate ??
manager.turn_count         ?  ?????
manager.clear_history()   ?  TaoLoop.reset() ???
```

---

## Persona ?????

??????**???**???????**???**???????? `PersonaManager` ?????

```
profile/store.py
  profile.json / skills.json / reflection.txt
         ?
  PersonaProfile           SkillsLibrary
  ?????????          ????????
  reflection: str
  ??????????
         ?
         ?
  EvolutionEngine.run()???????
    ? LLM ProfileDelta ? profile ????? N ??
    ? LLM SkillDelta   ? skills ????? N ??
    ? LLM reflect      ? reflection ???? M ??

  preference/????????? preference.json?
    PreferenceUpdater.update(Q, A)
      ?? LLM ?? ? ?? mood / topic_interests / style_shifts
         ??? L3 ??????? PreferenceBlock ?? Prompt?
          ?
  PersonaManager.all_blocks()
    ? [ProfileBlock, SkillsBlock?, ReflectionBlock?, PreferenceBlock?]
    ? ?? system prompt

  PersonaManager.bias_query(question)
    ? question + " " + topic_interests   ? L3 ????
```

### ??? vs ?????

| ?? | ?????????| ?????????|
|---|---|---|
| ?? | profile?skills?reflection | mood?topic_interests?style_shifts |
| ?? | LLM ? N ????????| LLM ??????????|
| ??? | ? ???? | ? preference.json |
| L3 ?? | ????? | ?????? |
| Prompt ?? | ???? | ? neutral ??? |

??????? [persona/README.md](./persona/README.md)?

---

## Trace ???

```
TraceStore.write(question, answer, steps)
  ?? .react/traces/{YYYYMMDD_HHMMSS}_{slug}.json
       {
         "id": "uuid",
         "timestamp": "...",
         "question": "...",
         "answer": "...",
         "steps": [{ "index", "thought", "action", "action_input", "observation" }]
       }
```

`processor.trace` ??????????????? finish ??????? `trace_store.write`?

---

## ConvLoop???????

```python
ConvLoop(tao: TaoLoop)

# ???
conv.stream(question)   ? ?? tao.stream()

# ?????? WebSocket ????????
conv.post_process()     ? tao.post_process()

# ????
conv.reset()            ? tao.reset() ? manager.clear_history() + ????

# ?????? JSON ?????????
conv.restore(messages)
  ?? ???? user/assistant ? manager.add_turn()
     ??????????

conv.turn_count         ? tao._manager.turn_count
```

---

## ??????

| ???? | ???? | ???? |
|---|---|---|
| `Step` | `memory/memory.py` | tao ? processor.add ? short/medium ? MemoryResult.short_term |
| `MemoryResult` | `memory/processor.py` | processor.recall ? build_messages?? milestone ???|
| `StaticPromptParts` | `prompt/manager.py` | post_process ? _static_cache ? build_messages_from_static |
| `list[BaseMessage]` | LangChain | build_messages ? llm.stream_generate_messages |
| `TaoEvent` | `tao.py` | TaoLoop.stream ? ConvLoop.stream ? webui/??? |
| `MemoryEntry` | `memory/long_term/store.py` | LongTermStore.add ? FAISS + memories.json |
| `MilestoneEntry` | `memory/milestone/entry.py` | ImportanceScorer.score ? MilestoneStore.add ? milestones.json |
| `ProfileDelta` | `persona/profile/evolver.py` | PersonaEvolver.evolve_profile ? _apply_profile_delta |
| `SkillDelta` | `persona/profile/evolver.py` | PersonaEvolver.evolve_skills ? _apply_skill_delta |
| `RecentPreference` | `persona/preference/recent.py` | PreferenceUpdater.update ? preference.json |

---

## ????

```
TaoConfig
  ??? max_steps: int = 10
  ??? finish_action: str = "finish"
  ??? memory: MemoryConfig
  ?     ??? short_term:  ShortTermMemoryConfig
  ?     ?     ??? enabled: bool = True
  ?     ?     ??? max_turns: int = 10
  ?     ?     ??? max_tokens: int = 2048
  ?     ??? medium_term: MediumTermMemoryConfig
  ?     ?     ??? enabled: bool = True
  ?     ?     ??? distill_trigger_steps: int = 4
  ?     ?     ??? max_distillate_tokens: int = 400
  ?     ??? long_term:   LongTermMemoryConfig       ? L3 ????
  ?     ?     ??? enabled: bool = False
  ?     ?     ??? load_from_disk: bool = False
  ?     ?     ??? memory_dir: str = ".react/memory"
  ?     ?     ??? top_k: int = 5
  ?     ?     ??? model_name_or_path: str = "BAAI/bge-small-zh-v1.5"
  ?     ?     ??? consolidation_k: int = 0   ? 0=???N=?N?????
  ?     ?     ??? retrieve: RetrieveConfig
  ?     ?           ??? light_top_k/min_score, heavy_top_k/min_score ...
  ?     ?           ??? timeline_top_k: int = 5   ? ??????
  ?     ?           ??? profile_query: str = "???? ??..."
  ?     ??? milestone:   MilestoneConfig             ? L2 ???
  ?           ??? enabled: bool = False
  ?           ??? milestone_dir: str = ".react/milestones"
  ?           ??? max_milestones: int = 50
  ?           ??? importance_threshold: float = 0.6
  ?           ??? max_keywords: int = 5
  ?           ??? top_k_retrieve: int = 2
  ?           ??? prompt_header: str = "## ?????"
  ??? prompt: PromptConfig
  ?     ??? lang: str = "en"   # "en" | "cn"
  ??? trace: TraceConfig
  ?     ??? enabled: bool = True
  ?     ??? trace_dir: str = ".react/traces"
  ??? persona: PersonaConfig
        ??? enabled: bool = False
        ??? persona_dir: str = ".react/persona"
        ?   Profile??????
        ??? max_profile_chars: int = 500
        ?   LLM ????
        ??? evolution_enabled: bool = False   ? ????? LLM ??
        ??? evolve_interval: int = 1          ? ? N ??? profile + skills
        ?   ???
        ??? skills_enabled: bool = True
        ??? max_skills: int = 50
        ??? max_skills_in_prompt: int = 5
        ??? max_skills_chars: int = 600
        ?   ???IROTE?
        ??? reflection_enabled: bool = False
        ??? reflect_interval: int = 3
        ??? max_reflection_chars: int = 400
        ?   ?????????
        ??? preference_enabled: bool = True
        ??? preference_window_days: int = 30
        ??? max_preference_topics: int = 10
        ??? preference_update_every_n: int = 3
        ??? max_preference_chars: int = 400
```

---

## ??????

```
.react/
??? history/              ? webui ?????/api/history ???
?   ??? {uuid}.json
??? memory/               ? L3 ?????LongTermStore?
?   ??? memories.json
?   ??? memory_index.faiss
??? milestones/           ? L2 ????MilestoneStore?
?   ??? milestones.json
??? persona/              ? ?????????
?   ??? profile.json      ? PersonaProfile?ProfileStore?
?   ??? skills.json       ? SkillsLibrary?ProfileStore?
?   ??? reflection.txt    ? ????????ProfileStore??????
?   ??? preference.json   ? ?????PreferenceStore?
??? traces/               ? ??????TraceStore?
    ??? {timestamp}_{slug}.json
```

---

## ?????

- [prompt/README.md](./prompt/README.md) ? Prompt ?????
- [memory/README.md](./memory/README.md) ? ??????
- [action/README.md](./action/README.md) ? ???????
- [persona/README.md](./persona/README.md) ? ????????
